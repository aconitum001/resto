class DealModel {
  final String tag, title, description, backgroundImage, mainImage;

  DealModel({
    required this.tag,
    required this.title,
    required this.description,
    required this.backgroundImage,
    required this.mainImage,
  });
}
